export interface Expense {
  id: number,
  amount: number,
  description: string,
  category: string,
  categoryId: number,
  createdAt: string,
  updatedAt: string,
  budget: string;
  budgetId: number;
}

